$(".favorite-btn").html("<%= j(render 'publics/favorites/favorite-btn', post: @post) %>");
<!--↓@book.idを入れることで一つのbookを指定している-->
$("#post_<%= @post.id %>").html("<%= j(render 'publics/favorites/favorite-btn', post: @post) %>");